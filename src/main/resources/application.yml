micronaut:
  application:
    name: catalogue
    instance:
      id: ${random.shortuuid}
  config-client:
    enabled: true
db:
  name: 'catalogue'
  collection: 'products'
netty:
  default:
    allocator:
      max-order: 3
endpoints:
  health:
    enabled: true
    sensitive: false