micronaut:
  application:
    name: catalogue
  security:
    enabled: false
  config-client:
    enabled: false
consul:
  client:
    registration:
      enabled: false
    config:
      enabled: false